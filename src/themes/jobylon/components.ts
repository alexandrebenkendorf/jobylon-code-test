import { colors } from './colors';

export const components = {
  body: { backgroundColor: '#FAFAFA', color: colors.textBody },
  header: { backgroundColor: colors.white, color: colors.textHeading },
  footer: { backgroundColor: colors.primary, color: colors.white },
};
